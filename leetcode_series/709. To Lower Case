class Solution {
public:
    string toLowerCase(string A) {
        string P;
        for(int i=0;i<A.size();i++)
        {
            if(A[i]>='A' && A[i]<='Z')
                A[i]+=32;
            
        }
        return A;
    }
};
