class Solution {
public:
    string defangIPaddr(string P) {
        string A;
        A="";
        int i,j,k,n= P.size();
        for(i=0;i<n;i++)
        {
            if(P[i]=='.')
            {
             A+="[.]";
            }
            else A+=P[i];
        }
        return A;
    }
};
