class Solution {
public:
    int xorOperation(int n, int start) {
        int i,j,k;
        k=0;
        for(i=0;i<n;i++)
        {
            k^= (start + 2*i);
        }
        return k;
    }
};
